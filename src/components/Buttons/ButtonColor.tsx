import React from "react";

export default function ButtonColor(props) {
  const { size, color, text, textSize } = props;
  // define size s rg m
  // define text size s rg m
  return <button>{text}</button>;
}

const Button() {
    
}